{# templates/admin/field/viz_config.html.twig #}
<div class="viz-config-field">
    {# Display the JSON configuration as a pre-formatted block #}
    <pre style="white-space: pre-wrap; word-wrap: break-word; font-size: 0.8em; background-color: #f8f9fa; border: 1px solid #e9ecef; padding: 10px; border-radius: 4px;">{{ field.value | json_encode(constant('JSON_PRETTY_PRINT')) }}</pre>
</div>